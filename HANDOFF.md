# ame-dev-site 引き継ぎメモ

> 次のセッションはこのファイルを最初に読む。文脈ゼロから再開できるよう維持する。
> 製品本体(Drive DAM)の最新は `~/Documents/claude-private/drive-dam/HANDOFF.md`。

## 0. 次の Claude へ(2026-07-20 セッション終了時の状態)

**Astro 実装済みの Drive DAM LP をブラッシュアップし、モバイル対応・SEO 整備まで完了**。
本セッション 2 コミット(74cc677 / 次コミット)で本番公開済み。デザインコンセプトは引き続き
**「Paper & Ink + Blue Light」**だが、紙色は **クールグレー(`--paper:#F6F8FA`)**に変更済。
実装は `src/pages/drivedam/index.astro` + `src/styles/drivedam.css` が一次ソース
(design-poc は過去の PoC 置き場として残置)。

**本セッションの変更(2026-07-20・2 コミットに分割)**:

### 前半コミット 74cc677(見た目・コピー中心)
1. **背景色をクールグレーに変更**: `--paper` `#F7F5F0→#F6F8FA`、`--paper-deep`
   `#F0EDE6→#EEF1F5`。ヘッダー scrolled 背景など rgba ハードコード箇所(計 5 箇所)も追随して統一。
2. **スクショ 3 枚を更新版に差し替え**: hero-main / func-files / func-thumbs
   (`design-poc/assets/` の更新版から反映)。実寸が 2556×1595 に微変化していたため
   HTML の `width`/`height` 属性も合わせて修正。
3. **ヘッダーにページ内メニュー追加**: 「Drive DAM とは/特徴/機能/価格/FAQ」5 項目のアンカー。
4. **カード内配置の微調整**: 「プレビューから即ダウンロード」拡大(`width 80→88%`)、
   「複数選択で一括編集」上寄せ(`top 11→4%`)。
5. **「安心設計 → 価格」の演出変更**: グラデーション帯は不採用。**墨面を角丸パネルとして
   紙の上に置く構図**に変更(`margin-top` + `margin-inline` + `border-radius`)。
6. **フッターを整理**: 「対応ストレージ」を「Google Drive などエクスプローラー上で利用できる
   オンラインストレージ、社内ファイルサーバー、ローカルフォルダに対応」の説明文に変更。
   「こんなチームに」列は削除、対応ストレージ列を右側に拡大。
7. **コピー修正 3 件**(3.5 節に反映済み):
   - サムネイル高速表示: 「初回のフォルダ登録時にサムネイルを作成してローカルにコピー」の仕組み説明を明確化
   - 絞り込み・検索: 本文簡潔化 + ファイルパス検索に言及。OCR 非対応の注記は `<span class="func-note">` で小さく薄い別行に分離
   - タグ共有: 「自分だけのお気に入り登録」に言及。アクセス権の話は安心設計と重複するため削除

### 第 2 コミット(モバイル・SEO 中心)
8. **モバイルハンバーガーメニュー実装**: 1024px 以下でヘッダー右端に 3 本線ボタン。
   タップで全画面オーバーレイ(rgba(246,248,250,.98) + backdrop-filter blur)を展開、
   5 項目メニューを段階的にフェードイン、下部に「近日 Microsoft Store で公開予定」表記。
   ESC・再タップ・リンクタップで閉じる。展開中は body スクロール禁止。
9. **フォント self-host 化**: Google Fonts CDN を撤去。`@fontsource/inter` +
   `@fontsource/noto-sans-jp` を npm install し、Base.astro と DrivedamLayout.astro の
   フロントマターで import。unicode-range サブセット分割 + `font-display:swap`。
   ハブ側は Inter のみ、Drive DAM LP は Inter + Noto Sans JP の 400/500/600/700。
10. **SEO 整備**: 両 Layout に og:image (hero-main.png 暫定利用) + og:image:width/height +
    twitter:card (summary_large_image) + twitter:title/description/image +
    meta theme-color (#F6F8FA) + apple-touch-icon (favicon.svg で代替) を追加。

### 第 3 コミット(コピー調整・安心設計セクション廃止・フッター再編)
11. **コピー修正 4 件**(3.6 節に反映済み): すべてのファイルを可視化(画像以外はサムネイル
    非対応の注記を追加)/ 書き出し(トリミング)の説明をリライト(トリミング+リサイズ同時に
    言及)/ 「誤って消す心配がない設計」→「元のフォルダ・ファイルを編集しない安心設計」に
    見出し・本文変更。
12. **「安心設計」セクション(assure)を丸ごと削除**: 4 項目のうち「削除機能はありません」は
    そのほかの機能カードでカバー済み、「画像ファイルは変更されません」「共有範囲はアクセス権
    のまま」は FAQ でカバー済み、「やめても、データは残ります」は新規 FAQ として移設。
    CSS の `.assure-*` クラス・関連コメントも削除。
13. **FAQ を大幅改訂(5 問 → 6 問)**: 「アンインストール後のデータ」を新規追加(旧「やめても
    データは残ります」の移設先。設定のエクスポート/インポートにも言及)、「Mac 対応」の
    Q/A をリライト(共有フォルダ上でサムネイル等は見えるがアプリ利用不可、と明確化)、
    「データを完全に削除する方法」を新規追加(`_dam`/`_dam_thumbs` フォルダの手動削除手順)、
    「個人開発のリスク」は削除(強調不要と判断)。3.9 節に最新 6 問を転記済み。
14. **ヘッダーロゴのリンク先変更**: `/`(ame_dev ハブ)→ `/drivedam/`(自ページ)。
15. **フッター再編**: ブランド名にアプリアイコン追加、説明文をヒーローのサブコピーと
    同一の文言に差し替え、「© ame_dev」を `/` へのリンク化。プライバシーポリシーは
    ame_dev 共通ページのため最下部エリアに残す方針を再確認(一時的に本体グリッドへ
    移動したが、あめさんの指摘で最下部に戻した)。

**次セッションの最初の作業**:
1. **専用の og:image を作る**(現状 hero-main.png を暫定利用中。1200×630 の SNS 特化画像に差し替えると SNS シェア時の見栄えが上がる)
2. **ストア公開時の CTA 差し替え**: 「近日 Microsoft Store で公開予定」は 3 箇所
   (Header・hero-cta・price-cta+ハンバーガーオーバーレイ)。ストア URL 確定後に一気に置換。
3. **JSON-LD (SoftwareApplication schema) の追加**: ストア公開後、価格・レビュー等を含めて追加すると Google 検索でリッチスニペット化
4. **引き続きあめさんの指摘反映**(90 点目標)
5. `func-download.png` は現在未使用(継続保留)

## 1. プロジェクト概要

- **何**: ame_dev ブランドの公式サイト。ハブ(`/`)+ Drive DAM ランディング(`/drivedam/`)。
- **目的**: Drive DAM の製品ランディング兼、将来製品を並べる ame_dev ハブ。
- **スタック**: Astro 7(minimal / TypeScript strict)。外部フォント・解析・Cookie なし。
- **公開先(予定)**: GitHub Pages User Site `hello-amedev/hello-amedev.github.io`(public、未作成)。
  User Site なのでルート(`/`)配信 = `base` 調整不要。
- **設置場所**: `~/Documents/claude-private/ame-dev-site/`(**worktree 運用外**。実質 main 側で作業)。

## 2. 現在の状態(2026-07-20)

- **公開済み**: `https://hello-amedev.github.io/`(ハブ)/ `/drivedam/`(LP)/ `/privacy/`(ポリシー)
- **GitHub リポジトリ**: `hello-amedev/hello-amedev.github.io`(Public User Site)
- **GitHub Pages Source**: GitHub Actions(`build_type: workflow`・main push で自動デプロイ)
- **最新コミット**: 本セッションのブラッシュアップ分(背景色・スクショ 3 枚・ヘッダーメニュー等)
  を push 済み。詳細は `git log` 参照。
- **design-poc/・copy/ は .gitignore で非追跡化**(内部 PoC 資材はローカルのみ)
- **dev サーバー起動中の場合あり**: 作業終了時は `npx astro dev stop` を忘れずに

### スクショ資材の全リスト(2026-07-19 時点・`design-poc/assets/`)

| ファイル | サイズ | 用途 |
|---|---|---|
| hero-main.png | 2558×1597 | ヒーロー(ライブラリ全景・1件選択中パネル・お気に入り星と用途タグ) |
| func-thumbs.png | 2558×1597 | 主要機能 01: サムネ一覧+ショートカット一覧 |
| func-search.png | 1872×1345 | 主要機能 02: 保存条件から 3 条件で絞込 |
| func-share1.png | 890×360 | 主要機能 03(share パネル): 使用可否バッジ 3 件 |
| func-share2.png | 547×1121 | 主要機能 03(share パネル): 詳細パネル縦長 |
| func-files.png | 2558×1597 | 検索・表示: PDF/MP4/XLSX 混在 |
| func-folder.png | 1999×1479 | 検索・表示: 下位フォルダ含めるオン |
| func-searchsave.png | 1998×781 | 検索・表示: 条件保存名+呼び出し |
| func-zoom.png | 2556×1595 | 検索・表示: プレビュー・原寸(ダーク背景) |
| func-download.png | 1250×702 | **未使用**(現在は zoom を再利用) |
| func-export1.png | 945×906 | 書き出し・DL: 書き出しモーダル全体 |
| func-export2.png | 1261×1309 | 書き出し・DL: トリミング書き出しモーダル全体 |
| func-bulc.png | 553×1114 | 整理: 10 件選択中の一括編集パネル縦長 |
| func-rename.png | 978×896 | 整理: 一括リネーム連番タブ |
| func-move.png | 803×964 | 整理: 移動先ツリー |
| func-tagmanager.png | 1576×1121 | チーム: タグ管理モーダル |
| func-compe.png | 945×533 | チーム: 競合解決ダイアログ |
| func-safe1.png | 537×213 | チーム: 非表示ボタン |
| func-safe2.png | 1524×705 | チーム: 非表示ファイル一覧 |
| func-dark.png | 2556×1595 | 外観: ダークモード全画面 |
| app-icon.png | 256×256 | ヘッダー・図解②・価格エリア |

## 3. 確定済みコピー一式(本ファイルが一次ソース・2026-07-19 更新)

### 3.1 ヘッダー
- **左(ロゴ)**: `/drivedam/`(自ページ)にリンク。ame_dev ハブへは飛ばない(2026-07-20 変更)
- **右**: 「近日公開予定」(静的表示・リンクなし)。1024px 以下ではハンバーガーメニューに統合

### 3.2 ヒーロー
- **eyebrow**: なし(削除済み)
- **メイン**: 数千枚の画像から、目的の **1 枚**へ。<br>ひとりでも、チームでも。
- **サブ**: ローカルフォルダでも、オンラインストレージ等でも**そのまま使える**デジタルアセット管理。<br>共有フォルダを通して、チームでの画像管理・活用を効率化します。
- **CTA**: 「近日 Microsoft Store で公開予定」(単一非活性ボタン)
- **ノート**: v0.13.1(2026-07-18)・Windows 10 以降対応(Eagle 方式)

### 3.3 Drive DAM とは(about)
- **見出し**: 広報やマーケティングのための、<br>画像管理ツール。
- **本文**: Drive DAM は、画像 1 枚ごとに小さなサムネイルと設定ファイルをオンラインストレージ側に作成することで、**チームでの画像管理と活用をスマートに実現する**ツールです。数万枚の画像を、複数人で、安全かつ高速に管理できます。ローカルフォルダでの利用にも対応しています。
- **図解キャプション**: 画像 1 枚ごとに、十数 KB 程度のサムネイル・設定ファイルを作成。これを起動時に読み込むことで高速で効率的な管理・活用を実現します。画像ファイルには手を加えず、安全です。

### 3.4 3 つの特徴(見出しのみ、本文は不変)
- 数万枚の画像から、高速検索。
- ローカルでも、オンラインストレージでも。
- タグや使用条件を、チームで共有。

### 3.5 主要機能(3・スクロール表示・2026-07-20 更新)
- **サムネイルを高速表示**: 数万枚のフォルダでも、開いてすぐに一覧できます。
  初回のフォルダ登録時にサムネイルを作成してローカルにコピーすることで、高速な表示を
  実現します。チームメンバーは同じサムネイルを利用でき、効率的に画像を検索・活用できます。
- **絞り込み・検索**: キーワード・タグ・色・撮影日・使用可否・お気に入りなど、さまざまな条件で
  画像の検索・絞り込みが可能です。ファイルパスでも検索できるので、フォルダ名の文字列でも
  画像を探せます。(小さく薄い別行)※画像内の文字認識等には対応していません
- **タグ・色・使用可否をチームで共有**: タグやメモなどの情報は、共有フォルダを介してメンバー
  全員が利用できます。使用条件(要許諾・使用不可など)も共有でき、画像の使用に関するミスを
  防げます。他のメンバーに共有されない、自分だけのお気に入り登録も可能です。

### 3.6 そのほかの機能(5 カテゴリ・12 機能・2026-07-20 一部改訂)
基本は 2026-07-19 セッションの再編構成(HTML 実装を一次ソースとする)。以下 3 件は本文を改訂済み:
- **すべてのファイルを可視化**: 画像管理が主機能ですが、それ以外のファイルも種別を表示し、
  タグやメモを付けて管理できます。(小さく薄い別行)※画像以外のサムネイル表示には未対応です。
- **書き出し(トリミング)**: 正方形・SNS向けなどのプリセットや任意の比率で、トリミングと
  同時にリサイズして書き出せます。複数枚をまとめて処理することもできます。
- **元のフォルダ・ファイルを編集しない安心設計**(旧「誤って消す心配がない設計」から改題):
  元の画像フォルダのファイルは削除できないようになっています。複数人で共有フォルダを扱う
  時にも安心な設計です。(小さく薄い別行)※不要なファイルについては、自分の一覧上で
  非表示にする機能があります。

### 3.7 安心設計(assure セクション。2026-07-20 廃止)
旧 4 項目は以下へ移設・統合済み。セクション自体は HTML/CSS から削除:
- 削除機能はありません → 3.6「元のフォルダ・ファイルを編集しない安心設計」カードに統合
- 画像ファイルは変更されません → 3.9 FAQ「画像ファイルは変更されますか?」でカバー
- 共有範囲は、フォルダのアクセス権のまま → 3.9 FAQ「共有フォルダのアクセス権がない人にも〜」でカバー
- やめても、データは残ります → 3.9 FAQ「Drive DAMをアンインストールしたら〜」に発展的に移設

### 3.8 価格
- **タイトル**: 30 日間、無料で試せます。
- **リード**: 試用中の機能制限はありません。チームで一通り使ったうえで、**買い切り(目安 2,000 円台・1 ユーザー 1 ライセンス)**で導入できます。月額課金はありません。
- **CTA**: 「近日 Microsoft Store で公開予定」(単一非活性ボタン)
- **メタ**: 月額課金なし・Windows 専用

### 3.9 FAQ(6 問・2026-07-20 大幅改訂)
1. **画像ファイルは変更されますか?** いいえ。タグやメモは画像と同じフォルダ内の `_dam/` に
   保存し、画像ファイルそのものには手を加えません。
2. **Drive DAMをアンインストールしたら、タグやメモはどうなりますか?** タグ・メモはローカルや
   共有フォルダ内の `_dam` フォルダに保存されるため、Drive DAMをアンインストールしてもそのまま
   残ります。チームメンバーの利用には影響ありません。再インストールしてもう一度フォルダを
   登録すれば、同じ状態で再開できます。また、設定のエクスポート・インポートを利用すれば、
   フォルダの登録状態や、ブックマークも復元できます。
3. **共有フォルダのアクセス権がない人にも、タグやメモは見えますか?** 見えません。共有フォルダの
   アクセス権限が、そのままタグ・メモの共有範囲になります。
4. **AI で自動的にタグ付けされますか?** いいえ。タグは手作業で付け、チームで共有して育てる
   運用です。自動分類による意図しない整理はありません。
5. **Macには対応していますか?** Drive DAMはWindows用アプリです。Macには現在対応していません。
   生成されたサムネイル・設定ファイルは共有フォルダ上に表示されますが、Drive DAMを利用する
   ことはできません。Mac版については、今後リリースされる可能性があります。
6. **サムネイルやタグのデータを完全に削除する方法は?** フォルダを登録しているDrive DAMユーザー
   が居ない状態で、`_dam` フォルダおよび `_dam_thumbs` フォルダを削除してください。フォルダ数が
   多い場合は、同名フォルダを検索してすべて削除してください。

(削除: 「個人開発のようですが、サービス終了のリスクは?」→ わざわざ個人開発を強調する
必要はないと判断)

### 3.10 トーン規定の要点(コピーに適用済み)
- 事実を淡々と説明する敬体(です・ます調)
- 「ひとつだけ」「合言葉」「安心して」「大切な」「資産」「事故」「ばっちり」は使わない
- **「被せる」は禁則・「明朝見出し」は禁則**(念押し済み)
- ヒーローのキャッチコピーのみ販促文脈で印象に残る表現を許容
- 絵文字は使わない、強調は太字、ファイル名・キー操作は `code` 書式
- **タグの例示は「WEB掲載」など具体・分野的なもので**(「屋外」だとカテゴリ例に見えて伝わりにくい・2026-07-19)

### 3.11 フッター(2026-07-20 更新)
- **ブランド**: アプリアイコン + 「Drive DAM」。説明文はヒーローのサブコピーと同一
  (「ローカルフォルダでも、オンラインストレージ等でもそのまま使えるデジタルアセット管理。
  共有フォルダを通して、チームでの画像管理・活用を効率化します。」)
- **対応ストレージ列**: 3.6 の説明文と同一(サービス名列挙ではなく説明文形式)
- **最下部**: 「© ame_dev」(`/` へのリンク)+「プライバシーポリシー」(`/privacy/` へのリンク)。
  プライバシーポリシーは ame_dev サイト共通のページなので、Drive DAM 固有のグリッドではなく
  最下部エリアに置く(2026-07-20 に一度本体グリッドへ移動したが、あめさんの指摘で
  最下部に戻した。**Drive DAM 固有ではない共通導線は最下部、というルールを踏襲**)

## 4. 次回着手するなら (2026-07-19 更新)

優先順:

1. **あめさんの再採点 → 90 点への磨き込み**(2026-07-18 時点で 80 点、本セッションで大幅拡張)
2. **細部調整**: 気になる箇所の指摘反映
   - 3 特徴図解の Google Drive/Dropbox 以外のアイコン(Box・フォルダ・サーバー)の詰め
   - グリッド 12 機能のカード内配置微調整
   - カテゴリ順序・見出し文言の再検討
3. **確定後**: Astro 本実装(ゼロベース上書き)→ モバイル最適化 → フォント self-host → git push
4. **git push 前の最終点検**:
   - `func-download.png` は現在未使用。削除判断
   - スクショにユーザー名が入っていないか再確認(現状 OK・全て `C:/広報素材/...`)
   - ヒーロースクショの実社名は前セッションで対応済み(ダミーライブラリ+subst)
5. **将来**: 英語対応(i18n)、ハブへの製品追加(Tsumist / FrameFit / Facilita)、
   Tsumist プライバシーポリシーのページ化(`copy/tsumist-privacy.md` にドラフト有)

## 5. 反省と方針(次回への申し送り)

### これまでのセッション概観
(過去詳細は割愛。必要なら 2026-07-17 版の HANDOFF を git history で参照)
- Session 1〜3 (2026-06-27〜28): テンプレ→signature探索→CSS擬似画面 いずれも見送り
- Session 4 (2026-07-12〜17): Paper & Ink + Blue Light コンセプト確立、実スクショ採用に振り切り
- Session 5 (2026-07-19): 実スクショ組み込み完了 / 機能 5 カテゴリ 12 機能に再編 / 3 特徴図解全書き直し
- Session 6 (2026-07-20): 背景色をベージュ→クールグレーに変更 / スクショ 3 枚差し替え /
  ヘッダーメニュー追加 / price セクションを角丸パネル化 / モバイルハンバーガー実装 /
  フォント self-host / og:image + SEO 整備 / 安心設計セクション廃止・FAQ 6 問に改訂 /
  コピー多数修正 / フッター再編(アイコン追加・説明文差し替え・リンク導線整理)

### 今回セッションで新規に確立した方針
1. **カード内での「画面の一部を覗く」演出は縁接続型で**: 全体を見せたい時は薄青パネル+
   浮かせるモーダル型、画面の一部を切り出す時は**カード縁で切れる**構図(ボーダー付き
   スクショが枠内に収まると「切り出し」に見えず、機能の見え方が損なわれる)
2. **カード比率は正方形 1:1 を主軸に**、横長画像は幅 1/2 段で対比配置。
   単独 2/3 幅は隣とのバランスが崩れやすい
3. **図解の線画言語**: 墨 1.4px ストローク+青(#4A7CE2)/赤(#B5544A)アクセント+
   グレー(#8B887F)のラベル文字。IntersectionObserver の `.is-in` で線が走る
4. **カテゴリ見出し**: 青いドット+日本語(20〜28px)。セクション見出しと設計言語を揃える
5. **ロゴ再現度の基準**: 参考画像のデフォルメ形状を線画で**構造まで再現**
   (Google Drive の底辺両端→反対斜辺への継ぎ目 2 本+底辺、
   Dropbox の 4 ダイヤ先端接続+開いた箱シェブロンなど)
6. **タグ例示は「WEB掲載」など具体分野的なもので統一**(3 特徴図解の右にも波及)
7. **「そのほかの機能」の締めはワイドバンドで**(ダークモード全画面のような横長の
   締めビジュアルは、3 カラムカードとは別リズムで置くと機能セクション全体が締まる)

### 継続方針(不変)
- 「実アプリ画面が見えるかどうか」を第一の品質ゲートに
- 両極端を避ける(淡々シンプル vs 個性的比喩)
- 製品画面のビジュアル化は「実スクショ or 完全再現」の二択、中間 NG
- **デザイン制作はメインモデルが直接**(エージェント委譲は平均に収束することが実測 2 回で判明)

## 6. 既知の落とし穴

- **User Site なので `base` は付けない**
- **CTA は非活性の単一ボタン**。ストア公開時に URL 化して差し替え(現状「近日公開予定」)
- **dev はデーモン常駐**。停止: `cd ~/Documents/claude-private/ame-dev-site && npx astro dev stop`
- **worktree 運用は実質してない**: このプロジェクトは main 側の作業ツリーで直接編集する
  運用が定着(前セッション終了時から `design-poc/` などが未コミットで積み上がっている)。
  worktree に入っても main 側ファイルを編集する形で問題ない。
  session-lifecycle.md の一般則より、HANDOFF「設置場所: worktree 運用外」の記述が優先
- **PoC scratchpad パスは worktree/セッション毎に変わる**。次セッションで必要な画像
  (例: 過去の PoC HTML)があれば、design-poc/ 側にコピー済みか要確認
- **「被せる」「明朝見出し」禁則**(2026-06-27 確定、2026-06-28 念押し、2026-07-19 も維持)
- **メイン擬似画面は「実スクショ or 完全再現」の二択**(2026-06-28 確定)
- **タグは「WEB掲載」など具体例で示す**(「屋外」だとカテゴリ例に見えず伝わらない・2026-07-19)
- **Astro 7 + Node 24 で Vite 8 系を直接呼ぶと `#module-sync-enabled` エラー**になることがある。
  Astro 経由で起動する分には問題ない
- **Google Chrome headless で画像確認する時**: `--force-prefers-reduced-motion` を付けないと
  IO 発火前 opacity:0 のせいで真っ白に撮れる。`--virtual-time-budget` は使わない(白紙化する)
- **subst で仮想ドライブを撮影する場合**: PowerShell では `%USERPROFILE%` が展開されないので
  `$env:USERPROFILE` を使う(または cmd で実行)

## 7. ビルド / デプロイ

```
cd ~/Documents/claude-private/ame-dev-site
npm run dev        # 開発(Astro デーモン)/ 停止: npx astro dev stop
npm run build      # dist/ に静的生成
npm run preview    # build 後のローカル確認
```

- デプロイは `main` push で GitHub Actions(`.github/workflows/deploy.yml`)が自動実行
- 公開先 URL: `https://hello-amedev.github.io/`(ルート) / `/drivedam/`(製品ページ)
