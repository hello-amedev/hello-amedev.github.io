# ame-dev-site 引き継ぎメモ

> 次のセッションはこのファイルを最初に読む。文脈ゼロから再開できるよう維持する。
> 製品本体(Drive DAM)の最新は `~/Documents/claude-private/drive-dam/HANDOFF.md`。

## 0. 次の Claude へ(2026-07-19 セッション終了時の状態)

**Drive DAM ページのリデザインが機能面まで拡張完了・全体像が固まった**。本命は
**`design-poc/drivedam.html`**(単一 HTML・file:// ダブルクリックで起動・約1700行)。
Astro 実装(`src/pages/drivedam/index.astro`)は旧版のまま。確定後にゼロベース上書きする方針は不変。

デザインの現在地: あめさん採点 60 → 75 → 80(2026-07-18) → **本セッションで機能セクション大幅拡張。
次セッションで再採点予定**。コンセプトは **「Paper & Ink + Blue Light」**(紙白 #F7F5F0 + 墨 3 階調の
エディトリアル骨格に、アプリの UI ブルー #4A7CE2 系を「ブランドの光」として重ねる)。

**本セッションの主な進展(2026-07-19)**:

1. **機能スクショの受領・組み込み完了**(全 17 枚、`design-poc/assets/`):
   hero-main / func-thumbs / func-search / func-share1 / func-share2 / func-files /
   func-folder / func-export1 / func-export2 / func-safe1 / func-safe2 / func-bulc /
   func-searchsave / func-tagmanager / func-compe / func-zoom / func-dark / func-rename /
   func-move / (未使用: func-download)。パス表示は subst で仮想 C ドライブ経由の撮影で
   ユーザー名なし(`C:/広報素材/...`)。
2. **機能セクションを 5 カテゴリ 12 機能に再編**(主要機能スクロール表示 3 つ+以下グリッド):
   - **検索・表示**(4): すべてのファイルを可視化 / プレビュー・原寸表示 /
     下位フォルダをまとめて表示 / 検索条件の保存(横長 2 枚を 1/2+1/2 で並置)
   - **書き出し・ダウンロード**(3): プレビューから即ダウンロード /
     書き出し(リサイズ・一括) / 書き出し(トリミング)
   - **整理・ファイル操作**(3・新設): 複数選択で一括編集 / 一括リネーム / ファイルの移動
     ※リード文で「タグ・色・メモはファイル名と結び付いて管理されている。DriveDAM 上で
     操作することで関連付けを保つ」と注意喚起(エクスプローラーで直接変更禁止)
   - **チームでの運用**(3): タグ・タググループの管理 / 編集の競合を検知・解決 /
     誤って消す心配がない設計
   - **外観**(1・ワイドバンド): ダークモードにも対応
3. **3 つの特徴の図解を全書き直し**(SVG 線画統一・「Drive DAM とは」と同じ言語):
   - 左: 検索バー+チップ(WEB掲載/SNS投稿+AND|OR+除外「期間限定」赤+色「赤」)+件数 12/12,179
   - 中: 中央にアプリアイコン、左右均等配置(ローカル+サーバー/GDrive+Box+Dropbox)。
     各ロゴを 40px の作り込み線画に(白ベタ廃止・Google Drive は三角+X交差+底辺の 3 線、
     Dropbox は 4 ダイヤの先端接続+下の開いた箱シェブロンで再現度アップ)
   - 右: 共有フォルダ樹形図 → A/B/C(人型アイコン)3 行に同じタグ(WEB掲載)+色+★
4. **ヒーロー再構成**: eyebrow 削除、サブコピーを「デジタルアセット管理」+効率化 の 2 行に、
   CTA を「近日 Microsoft Store で公開予定」の**単一非活性ボタン**に(公開通知系すべて削除)、
   注記を Eagle 方式「v0.13.1(2026-07-18)・Windows 10 以降対応」に変更
5. **カテゴリ見出しを大きく**(青いドット+日本語 20〜28px)、about 本文と fig-cap も更新
6. **カード内の見せ方に 2 系統を確立**:
   - **薄青パネル+浮かせるモーダル型**(書き出し・タグ管理・競合など): 全体が枠内に収まる
   - **カード縁で切れる「画面の一部を覗く」演出**(プレビューから即ダウンロード・
     複数選択で一括編集の 2 枚のみ): アプリ画面の続きが枠外にある感を出す

**次セッションの最初の作業**:
1. あめさんの再採点・指摘の反映(90 点目標)
2. 気になる箇所の微調整(必要に応じ)
3. 確定後: Astro 本実装(ゼロベース上書き)→ モバイル最適化 → フォント self-host → git push
4. `func-download.png` は現在未使用。assets 内に残置。削除するか判断

## 1. プロジェクト概要

- **何**: ame_dev ブランドの公式サイト。ハブ(`/`)+ Drive DAM ランディング(`/drivedam/`)。
- **目的**: Drive DAM の製品ランディング兼、将来製品を並べる ame_dev ハブ。
- **スタック**: Astro 7(minimal / TypeScript strict)。外部フォント・解析・Cookie なし。
- **公開先(予定)**: GitHub Pages User Site `hello-amedev/hello-amedev.github.io`(public、未作成)。
  User Site なのでルート(`/`)配信 = `base` 調整不要。
- **設置場所**: `~/Documents/claude-private/ame-dev-site/`(**worktree 運用外**。実質 main 側で作業)。

## 2. 現在の状態(2026-07-19)

- **公開済み**: `https://hello-amedev.github.io/`(ハブ)/ `/drivedam/`(LP)/ `/privacy/`(ポリシー)
- **GitHub リポジトリ**: `hello-amedev/hello-amedev.github.io`(Public User Site)
- **GitHub Pages Source**: GitHub Actions(`build_type: workflow`・main push で自動デプロイ)
- **最新コミット**: `32ef38a` Drive DAM ページの新デザインを Astro に本実装 + サイト公開の初期整備
- **design-poc/・copy/ は .gitignore で非追跡化**(内部 PoC 資材はローカルのみ)
- **dev サーバー停止済み**・**テレメトリ無効化済み**

### スクショ資材の全リスト(2026-07-19 時点・`design-poc/assets/`)

| ファイル | サイズ | 用途 |
|---|---|---|
| hero-main.png | 2558×1597 | ヒーロー(ライブラリ全景・1件選択中パネル・お気に入り星と用途タグ) |
| func-thumbs.png | 2558×1597 | 主要機能 01: サムネ一覧+ショートカット一覧 |
| func-search.png | 1872×1345 | 主要機能 02: 保存条件から 3 条件で絞込 |
| func-share1.png | 890×360 | 主要機能 03(share パネル): 使用可否バッジ 3 件 |
| func-share2.png | 547×1121 | 主要機能 03(share パネル): 詳細パネル縦長 |
| func-files.png | 2558×1597 | 検索・表示: PDF/MP4/XLSX 混在 |
| func-folder.png | 1999×1479 | 検索・表示: 下位フォルダ含めるオン |
| func-searchsave.png | 1998×781 | 検索・表示: 条件保存名+呼び出し |
| func-zoom.png | 2556×1595 | 検索・表示: プレビュー・原寸(ダーク背景) |
| func-download.png | 1250×702 | **未使用**(現在は zoom を再利用) |
| func-export1.png | 945×906 | 書き出し・DL: 書き出しモーダル全体 |
| func-export2.png | 1261×1309 | 書き出し・DL: トリミング書き出しモーダル全体 |
| func-bulc.png | 553×1114 | 整理: 10 件選択中の一括編集パネル縦長 |
| func-rename.png | 978×896 | 整理: 一括リネーム連番タブ |
| func-move.png | 803×964 | 整理: 移動先ツリー |
| func-tagmanager.png | 1576×1121 | チーム: タグ管理モーダル |
| func-compe.png | 945×533 | チーム: 競合解決ダイアログ |
| func-safe1.png | 537×213 | チーム: 非表示ボタン |
| func-safe2.png | 1524×705 | チーム: 非表示ファイル一覧 |
| func-dark.png | 2556×1595 | 外観: ダークモード全画面 |
| app-icon.png | 256×256 | ヘッダー・図解②・価格エリア |

## 3. 確定済みコピー一式(本ファイルが一次ソース・2026-07-19 更新)

### 3.1 ヘッダー
- **右**: 「近日公開予定」(静的表示・リンクなし)

### 3.2 ヒーロー
- **eyebrow**: なし(削除済み)
- **メイン**: 数千枚の画像から、目的の **1 枚**へ。<br>ひとりでも、チームでも。
- **サブ**: ローカルフォルダでも、オンラインストレージ等でも**そのまま使える**デジタルアセット管理。<br>共有フォルダを通して、チームでの画像管理・活用を効率化します。
- **CTA**: 「近日 Microsoft Store で公開予定」(単一非活性ボタン)
- **ノート**: v0.13.1(2026-07-18)・Windows 10 以降対応(Eagle 方式)

### 3.3 Drive DAM とは(about)
- **見出し**: 広報やマーケティングのための、<br>画像管理ツール。
- **本文**: Drive DAM は、画像 1 枚ごとに小さなサムネイルと設定ファイルをオンラインストレージ側に作成することで、**チームでの画像管理と活用をスマートに実現する**ツールです。数万枚の画像を、複数人で、安全かつ高速に管理できます。ローカルフォルダでの利用にも対応しています。
- **図解キャプション**: 画像 1 枚ごとに、十数 KB 程度のサムネイル・設定ファイルを作成。これを起動時に読み込むことで高速で効率的な管理・活用を実現します。画像ファイルには手を加えず、安全です。

### 3.4 3 つの特徴(見出しのみ、本文は不変)
- 数万枚の画像から、高速検索。
- ローカルでも、オンラインストレージでも。
- タグや使用条件を、チームで共有。

### 3.5 主要機能(3・スクロール表示)
- **サムネイルを高速表示**: 数万枚のフォルダでも、開いてすぐに一覧できます。
  誰かが一度生成したサムネイルをチーム全員で再利用し、ローカルにも控えるため、
  2 回目以降の表示はさらに速くなります。
- **絞り込み・検索**: キーワード・タグ・色・撮影日・使用可否・お気に入りを重ねて絞り込みます。
  条件を組み合わせるほど候補が減ります。検索対象はファイル名・タグ・メモです。
  画像内の文字(OCR)や画像認識による自動検索は行いません。
- **タグ・色・使用可否をチームで共有**: 付けた情報は共有フォルダを介してメンバー全員の画面に
  反映されます。使用可否(社外公開可・要許諾・使用不可など)も共有でき、判断のばらつきを抑えます。
  共有フォルダのアクセス権がない人には共有されません。

### 3.6 そのほかの機能(5 カテゴリ・12 機能)
上記 0 章「本セッションの主な進展」項目 2 と HTML 実装を一次ソースとする。

### 3.7 安心設計(assure)
- **削除機能はありません**: 見せたくない画像は「あとから戻せる非表示」で自分の一覧から外せます。
  他のメンバーの表示には影響しません。
- **画像ファイルは変更されません**
- **共有範囲は、フォルダのアクセス権のまま**
- **やめても、データは残ります**

### 3.8 価格
- **タイトル**: 30 日間、無料で試せます。
- **リード**: 試用中の機能制限はありません。チームで一通り使ったうえで、**買い切り(目安 2,000 円台・1 ユーザー 1 ライセンス)**で導入できます。月額課金はありません。
- **CTA**: 「近日 Microsoft Store で公開予定」(単一非活性ボタン)
- **メタ**: 月額課金なし・Windows 専用

### 3.9 FAQ(5 問・不変)

### 3.10 トーン規定の要点(コピーに適用済み)
- 事実を淡々と説明する敬体(です・ます調)
- 「ひとつだけ」「合言葉」「安心して」「大切な」「資産」「事故」「ばっちり」は使わない
- **「被せる」は禁則・「明朝見出し」は禁則**(念押し済み)
- ヒーローのキャッチコピーのみ販促文脈で印象に残る表現を許容
- 絵文字は使わない、強調は太字、ファイル名・キー操作は `code` 書式
- **タグの例示は「WEB掲載」など具体・分野的なもので**(「屋外」だとカテゴリ例に見えて伝わりにくい・2026-07-19)

## 4. 次回着手するなら (2026-07-19 更新)

優先順:

1. **あめさんの再採点 → 90 点への磨き込み**(2026-07-18 時点で 80 点、本セッションで大幅拡張)
2. **細部調整**: 気になる箇所の指摘反映
   - 3 特徴図解の Google Drive/Dropbox 以外のアイコン(Box・フォルダ・サーバー)の詰め
   - グリッド 12 機能のカード内配置微調整
   - カテゴリ順序・見出し文言の再検討
3. **確定後**: Astro 本実装(ゼロベース上書き)→ モバイル最適化 → フォント self-host → git push
4. **git push 前の最終点検**:
   - `func-download.png` は現在未使用。削除判断
   - スクショにユーザー名が入っていないか再確認(現状 OK・全て `C:/広報素材/...`)
   - ヒーロースクショの実社名は前セッションで対応済み(ダミーライブラリ+subst)
5. **将来**: 英語対応(i18n)、ハブへの製品追加(Tsumist / FrameFit / Facilita)、
   Tsumist プライバシーポリシーのページ化(`copy/tsumist-privacy.md` にドラフト有)

## 5. 反省と方針(次回への申し送り)

### これまでのセッション概観
(過去詳細は割愛。必要なら 2026-07-17 版の HANDOFF を git history で参照)
- Session 1〜3 (2026-06-27〜28): テンプレ→signature探索→CSS擬似画面 いずれも見送り
- Session 4 (2026-07-12〜17): Paper & Ink + Blue Light コンセプト確立、実スクショ採用に振り切り
- Session 5 (2026-07-19): 実スクショ組み込み完了 / 機能 5 カテゴリ 12 機能に再編 / 3 特徴図解全書き直し

### 今回セッションで新規に確立した方針
1. **カード内での「画面の一部を覗く」演出は縁接続型で**: 全体を見せたい時は薄青パネル+
   浮かせるモーダル型、画面の一部を切り出す時は**カード縁で切れる**構図(ボーダー付き
   スクショが枠内に収まると「切り出し」に見えず、機能の見え方が損なわれる)
2. **カード比率は正方形 1:1 を主軸に**、横長画像は幅 1/2 段で対比配置。
   単独 2/3 幅は隣とのバランスが崩れやすい
3. **図解の線画言語**: 墨 1.4px ストローク+青(#4A7CE2)/赤(#B5544A)アクセント+
   グレー(#8B887F)のラベル文字。IntersectionObserver の `.is-in` で線が走る
4. **カテゴリ見出し**: 青いドット+日本語(20〜28px)。セクション見出しと設計言語を揃える
5. **ロゴ再現度の基準**: 参考画像のデフォルメ形状を線画で**構造まで再現**
   (Google Drive の底辺両端→反対斜辺への継ぎ目 2 本+底辺、
   Dropbox の 4 ダイヤ先端接続+開いた箱シェブロンなど)
6. **タグ例示は「WEB掲載」など具体分野的なもので統一**(3 特徴図解の右にも波及)
7. **「そのほかの機能」の締めはワイドバンドで**(ダークモード全画面のような横長の
   締めビジュアルは、3 カラムカードとは別リズムで置くと機能セクション全体が締まる)

### 継続方針(不変)
- 「実アプリ画面が見えるかどうか」を第一の品質ゲートに
- 両極端を避ける(淡々シンプル vs 個性的比喩)
- 製品画面のビジュアル化は「実スクショ or 完全再現」の二択、中間 NG
- **デザイン制作はメインモデルが直接**(エージェント委譲は平均に収束することが実測 2 回で判明)

## 6. 既知の落とし穴

- **User Site なので `base` は付けない**
- **CTA は非活性の単一ボタン**。ストア公開時に URL 化して差し替え(現状「近日公開予定」)
- **dev はデーモン常駐**。停止: `cd ~/Documents/claude-private/ame-dev-site && npx astro dev stop`
- **worktree 運用は実質してない**: このプロジェクトは main 側の作業ツリーで直接編集する
  運用が定着(前セッション終了時から `design-poc/` などが未コミットで積み上がっている)。
  worktree に入っても main 側ファイルを編集する形で問題ない。
  session-lifecycle.md の一般則より、HANDOFF「設置場所: worktree 運用外」の記述が優先
- **PoC scratchpad パスは worktree/セッション毎に変わる**。次セッションで必要な画像
  (例: 過去の PoC HTML)があれば、design-poc/ 側にコピー済みか要確認
- **「被せる」「明朝見出し」禁則**(2026-06-27 確定、2026-06-28 念押し、2026-07-19 も維持)
- **メイン擬似画面は「実スクショ or 完全再現」の二択**(2026-06-28 確定)
- **タグは「WEB掲載」など具体例で示す**(「屋外」だとカテゴリ例に見えず伝わらない・2026-07-19)
- **Astro 7 + Node 24 で Vite 8 系を直接呼ぶと `#module-sync-enabled` エラー**になることがある。
  Astro 経由で起動する分には問題ない
- **Google Chrome headless で画像確認する時**: `--force-prefers-reduced-motion` を付けないと
  IO 発火前 opacity:0 のせいで真っ白に撮れる。`--virtual-time-budget` は使わない(白紙化する)
- **subst で仮想ドライブを撮影する場合**: PowerShell では `%USERPROFILE%` が展開されないので
  `$env:USERPROFILE` を使う(または cmd で実行)

## 7. ビルド / デプロイ

```
cd ~/Documents/claude-private/ame-dev-site
npm run dev        # 開発(Astro デーモン)/ 停止: npx astro dev stop
npm run build      # dist/ に静的生成
npm run preview    # build 後のローカル確認
```

- デプロイは `main` push で GitHub Actions(`.github/workflows/deploy.yml`)が自動実行
- 公開先 URL: `https://hello-amedev.github.io/`(ルート) / `/drivedam/`(製品ページ)
