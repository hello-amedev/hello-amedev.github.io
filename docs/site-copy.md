# site-copy.md — ame_dev 公式サイトの確定コピー

**このファイルがコピーの一次ソース。** 文言を変える時はここも直す。

例外が 2 つある:
- **英語コピー**の一次ソースは EN ページの HTML 実装(`src/pages/en/**`)
- **総合 TOP の作品情報**の一次ソースは `src/data/apps.ts`
  (ヒーローと Works カードの両方が参照するため、実装側に置いている)

## トーン規定

- 事実を淡々と説明する敬体(です・ます調)
- 使わない語: 「ひとつだけ」「合言葉」「安心して」「大切な」「資産」「事故」「ばっちり」
- **禁則**: 「被せる」/ 明朝の見出し
- ヒーローのキャッチコピーのみ、販促文脈で印象に残る表現を許容
- 絵文字は使わない。強調は太字、ファイル名・キー操作は `code` 書式
- **タグの例示は「WEB掲載」など具体・分野的なもので**
  (「屋外」だとカテゴリ例に見えて伝わりにくい)

## 総合 TOP(`/`)

### ヘッダー
`ame_dev`(ドット付き)+ ページ内メニュー `Works` / `AI for ART` / `About`。
640px 以下では `AI for ART` を隠す。

### ヒーロー
- **eyebrow**: ame_dev
- **キャッチ**: 自分や、自分のような誰かのための<br>アプリを作る。
- **サブ**: Windows アプリ・Chrome 拡張・Web アプリ、そしてデジタルアート。<br>ひとりでつくって、公開しています。

アイコンを選ぶと、この位置に選んだ作品の
「種別バッジ / 名前 / 一言 / リード文 / 入手先リンク」が入れ替わりで出る。
文面は `src/data/apps.ts`。

### Works
- **見出し**: Works(小さなラベルのみ。大きな日本語見出しは置かない)
- **リード**: 仕事や生活のなかで欲しいと思ったものを、ひとつずつ開発しています。
- **主力カード(Drive DAM)のキャッチ**: 数万枚の画像から、目的の 1 枚へ。<br>ひとりでも、チームでも。
  (Microsoft Store のメインビジュアルと同じ文言)

### AI for ART
- **ラベル**: AI FOR ART
- **ステートメント**: 競争や効率化のためではなく、AI をパートナーとして
  デジタルアートを共創する、個人的なプロジェクト。
- **主導線**: 「AI for ART を見る →」(ステートメントの直下に独立して置く)
- **作品**: 生命の夜 / A Night of Life。
  サブ「遺伝的アルゴリズムで変化し続ける光のアート」
- **作品本文**: 生命とは、絶えることなく「夜」を渡っていくプロジェクトである。
  その軌跡を、デジタルアートとして表出させることを試みた。
- **クレジット**: Partner — Claude Fable 5, Opus 4.8

### About
- **見出し**: About(小さなラベルのみ)
- **本文**: ame_dev は、ひとりでアプリを開発しているブランドです。
  「視覚的にわかりやすく」をテーマに、仕事や生活のなかで自分が欲しいと感じたものを
  AI と一緒に形にしています。
- **リンク**: X / BOOTH / amemitai@gmail.com

### 種別バッジの表記
`Windows アプリ` / `Chrome 拡張` / `スマートフォン用 WEB アプリ` / `準備中`

## Drive DAM LP(`/drivedam/`)

### ヘッダー
- **左(ロゴ)**: `/drivedam/`(自ページ)にリンク。ame_dev ハブへは飛ばさない
- **ページ内メニュー**: Drive DAM とは / 特徴 / 機能 / ダウンロード / FAQ
  (リンク先は `#price` のまま。金額表示のない CV エリアに「価格」は実態と
  合わないため「ダウンロード」に変更)
- **右**: 「Microsoft Store で入手」。1024px 以下はハンバーガーに統合
  (下部リンクは「Microsoft Store で公開中」)

### ヒーロー
- **eyebrow**: なし
- **メイン**: 数千枚の画像から、目的の **1 枚**へ。<br>ひとりでも、チームでも。
- **サブ**: ローカルフォルダでも、オンラインストレージ等でも**そのまま使える**
  デジタルアセット管理。<br>共有フォルダを通して、チームでの画像管理・活用を効率化します。
- **CTA**: バッジ型 `<StoreBadge>`「Microsoft Store で / 入手する」
  (EN は「Get it on / Microsoft Store」)
- **ノート**: v◯.◯.◯(日付)・Windows 10 以降対応
  ※バージョンは `src/data/releases.ts` から自動反映。直書きしない

### Drive DAM とは(about)
- **見出し**: 広報やマーケティングのための、<br>画像管理ツール。
- **本文**: Drive DAM は、画像 1 枚ごとに小さなサムネイルと設定ファイルを
  オンラインストレージ側に作成することで、**チームでの画像管理と活用を
  スマートに実現する**ツールです。数万枚の画像を、複数人で、安全かつ高速に
  管理できます。ローカルフォルダでの利用にも対応しています。
- **図解キャプション**: 画像 1 枚ごとに、十数 KB 程度のサムネイル・設定ファイルを作成。
  これを起動時に読み込むことで高速で効率的な管理・活用を実現します。
  画像ファイルには手を加えず、安全です。

### 3 つの特徴(見出しのみ)
- 数万枚の画像から、高速検索。
- ローカルでも、オンラインストレージでも。
- タグや使用条件を、チームで共有。

### 主要機能(3・スクロール表示)
- **サムネイルを高速表示**: 数万枚のフォルダでも、開いてすぐに一覧できます。
  初回のフォルダ登録時にサムネイルを作成してローカルにコピーすることで、
  高速な表示を実現します。チームメンバーは同じサムネイルを利用でき、
  効率的に画像を検索・活用できます。
- **絞り込み・検索**: キーワード・タグ・色・撮影日・使用可否・お気に入りなど、
  さまざまな条件で画像の検索・絞り込みが可能です。ファイルパスでも検索できるので、
  フォルダ名の文字列でも画像を探せます。
  (小さく薄い別行)※画像内の文字認識等には対応していません
- **タグ・色・使用可否をチームで共有**: タグやメモなどの情報は、共有フォルダを介して
  メンバー全員が利用できます。使用条件(要許諾・使用不可など)も共有でき、
  画像の使用に関するミスを防げます。他のメンバーに共有されない、
  自分だけのお気に入り登録も可能です。

### そのほかの機能(5 カテゴリ 12 機能)
HTML 実装を一次ソースとする。以下 3 件だけここに記録:
- **すべてのファイルを可視化**: 画像管理が主機能ですが、それ以外のファイルも
  種別を表示し、タグやメモを付けて管理できます。
  (小さく薄い別行)※画像以外のサムネイル表示には未対応です。
- **書き出し(トリミング)**: 正方形・SNS 向けなどのプリセットや任意の比率で、
  トリミングと同時にリサイズして書き出せます。複数枚をまとめて処理することもできます。
- **元のフォルダ・ファイルを編集しない安心設計**: 元の画像フォルダのファイルは
  削除できないようになっています。複数人で共有フォルダを扱う時にも安心な設計です。
  (小さく薄い別行)※不要なファイルについては、自分の一覧上で非表示にする機能があります。

### 価格
- **タイトル**: 30 日間、無料で試せます。
- **リード**: **サブスク不要、買い切りでずっと使えます。**(改行)
  30 日間すべての機能を無料でお使いいただけます。
- **CTA**: バッジ型 `<StoreBadge night>`(墨面用の明るい藍バリアント)
- **メタ**: ヒーロー下ノートと同一文言

### FAQ(6 問)
1. **画像ファイルは変更されますか?** いいえ。タグやメモは画像と同じフォルダ内の
   `_dam/` に保存し、画像ファイルそのものには手を加えません。
2. **Drive DAM をアンインストールしたら、タグやメモはどうなりますか?**
   タグ・メモはローカルや共有フォルダ内の `_dam` フォルダに保存されるため、
   アンインストールしてもそのまま残ります。チームメンバーの利用には影響ありません。
   再インストールしてもう一度フォルダを登録すれば、同じ状態で再開できます。
   また、設定のエクスポート・インポートを利用すれば、フォルダの登録状態や
   ブックマークも復元できます。
3. **共有フォルダのアクセス権がない人にも、タグやメモは見えますか?** 見えません。
   共有フォルダのアクセス権限が、そのままタグ・メモの共有範囲になります。
4. **AI で自動的にタグ付けされますか?** いいえ。タグは手作業で付け、
   チームで共有して育てる運用です。自動分類による意図しない整理はありません。
5. **Mac には対応していますか?** Drive DAM は Windows 用アプリです。
   Mac には現在対応していません。生成されたサムネイル・設定ファイルは
   共有フォルダ上に表示されますが、Drive DAM を利用することはできません。
   Mac 版については、今後リリースされる可能性があります。
6. **サムネイルやタグのデータを完全に削除する方法は?** フォルダを登録している
   Drive DAM ユーザーが居ない状態で、`_dam` フォルダおよび `_dam_thumbs` フォルダを
   削除してください。フォルダ数が多い場合は、同名フォルダを検索してすべて削除してください。

### フッター
- **ブランド**: アプリアイコン + 「Drive DAM」。説明文はヒーローのサブコピーと同一
- **対応ストレージ列**: サービス名の列挙ではなく説明文形式
- **最下部**: 「© ame_dev」(`/` へのリンク)+「プライバシーポリシー」。
  **Drive DAM 固有ではない共通導線は最下部**、というルール

## 更新履歴ページ(`/drivedam/releases/`)

- **URL**: 日 `/drivedam/releases/` / 英 `/en/drivedam/releases/`。
  将来ほかの製品が並んだときも `/<製品>/releases/` で揃うよう、
  サイト共通の `/releases/` にはしていない
- **見出し**: 「Drive DAM の更新履歴」/ "Drive DAM release notes"
- **リード**: 「Microsoft Store で公開した各バージョンの変更内容です。/
  更新は Microsoft Store から自動で配信されます。」
- **収録範囲**: **Store で一般に入手できたバージョンのみ**(v1.0.0 以降)。
  v0.9.x〜v0.13.x は社内試用配布のみなので載せない
- **文面**: Store の「このバージョンの最新情報」と同じもの。
  原典は drive-dam 側の `docs/store-listing.md`
- **導線**: ヒーロー下ノートと価格メタのバージョン表記(`.ver-link`)+
  フッターの「Drive DAM について」列。
  **ヘッダーのページ内メニューには入れない**(他の 5 項目がページ内アンカーのため)
- **JSON-LD は出さない**(SoftwareApplication を製品ページと二重に主張しないため)
